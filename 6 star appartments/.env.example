# Public Google Identity Services client ID only.
# Create it in Google Cloud Console and add http://localhost:3000
# and your production origin to the authorized JavaScript origins.
VITE_GOOGLE_CLIENT_ID=

# Server-side only. Never expose this value through Vite.
GOOGLE_CLIENT_ID=
